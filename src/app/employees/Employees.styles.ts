import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  addButton: {
    display: "block",
    marginLeft: "auto",
    marginRight: 32,
  },
  addIcon: {
    height: 24,
    width: 24,
    fill: theme.white,
  },
}));
